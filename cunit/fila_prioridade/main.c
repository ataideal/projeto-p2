#include <stdio.h>
#include <stdlib.h>



int main(int argc, char** argv) {
    /*priorityQueue * pq = createPriorityQueue();
    int element,p;
    scanf("%d %d",&element,&p);
    enqueue(pq,element,p);
    
    printf("%d",dequeue(pq));
    */
    return (EXIT_SUCCESS);
}

